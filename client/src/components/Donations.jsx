/*import { useState } from "react";
const Donations = () => {
  const [quantity, setQuantity] = useState("");
  const [money, setMoney] = useState("");
  return (
    <div className="flex w-full justify-center">
      <form className="flex flex-col max-w-3xl w-full gap-5 p-10 bg-neutral-900 rounded-md">
        <div className="text-center pb-5">
          <h1 className="text-5xl">Donate</h1>
        </div>
        <input
          className="p-2 bg-neutral-800"
          type="quantity"
          placeholder="Quantity"
          value={quantity}
          onChange={(e) => setQuantity(e.target.value)}
        />
        <input
          className="p-2 bg-neutral-800"
          type="money"
          placeholder="Money"
          value={money}
          onChange={(e) => setMoney(e.target.value)}
        />
        <button
          type="submit"
          className="p-2 bg-neutral-700 hover:bg-neutral-500 transition-all"
        >
          Donate
        </button>
      </form>
    </div>
  );
};

export default dontaions;*/
